<% provide(:title, 'All Episodes') %>
<h1>エピソード一覧</h1>
    


<%# <section class="episode_form"> %>
  <%= search_form_for @q do |f| %>
    <%= f.label :tags_name_cont, '資質検索' %>
    <%= f.collection_check_boxes :tags_id_in, Tag.all, :id, :name, {include_hidden: false} %>
    <br>
    <%= f.label :content_cont, 'エピソード検索' %>
    <%= f.search_field :content_cont %>
    <%= f.submit '検索' %>
    <%= link_to "リセット", episodes_path, class: "btn btn-outline-secondary" %>
  <% end %>
<%# </section> %>


<%= will_paginate unless @episodes.empty? %>

<ul class="episodes">
  <%= render @episodes %>
</ul>

<%= will_paginate unless @episodes.empty? %>
